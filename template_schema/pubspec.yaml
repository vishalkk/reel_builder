name: template_schema
description: Reel template schema models, serialization, and validation.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.8.0

dependencies:
  json_annotation: ^4.11.0

dev_dependencies:
  build_runner: ^2.4.15
  json_serializable: ^6.9.4
  lints: ^5.1.1
  test: ^1.25.15
